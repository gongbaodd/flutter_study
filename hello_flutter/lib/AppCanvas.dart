import 'package:flutter/material.dart';
import 'package:hello_flutter/CanvasPainter.dart';

class AppCanvas extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: "Canvas",
      home: MyCanvas(),
    );
  }
}
